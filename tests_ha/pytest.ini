; Config for the HA-harness tests only (CI runs `pytest tests_ha/`, which
; makes this directory the rootdir). The pure-DSP tests in tests/ are
; unaffected. asyncio_mode=auto lets the plain `async def` tests run without
; per-test markers, matching Home Assistant's own test setup.
[pytest]
asyncio_mode = auto
